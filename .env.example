DATABASE_URL=postgresql://shadow:REPLACE_PASSWORD@127.0.0.1:5432/shadow_life
SHADOW_DEV_AUTH=false
SHADOW_API_URL=http://127.0.0.1:8787
# Alternative production mode behind an authenticated, loopback reverse proxy.
# SHADOW_PROXY_AUTH_SECRET=REPLACE_WITH_RANDOM_32_BYTE_VALUE
# SHADOW_PROXY_SUBJECT_ID=subject_example
# Production Web login (values are deployment-specific and stay outside Git).
SHADOW_OIDC_ISSUER=https://id.example.com/
SHADOW_OIDC_AUDIENCE=shadow-life
SHADOW_OIDC_JWKS_URL=https://id.example.com/.well-known/jwks.json
# RFC 9068 access-token clients accepted by the Life API. Keep actual IDs in runtime config.
SHADOW_OIDC_ALLOWED_CLIENTS=REPLACE_ANDROID_CLIENT_ID,REPLACE_WEB_CLIENT_ID
# Exact opaque OIDC sub -> Life subject mapping; never infer this from email or username.
SHADOW_IDENTITY_SUBJECTS={"REPLACE_OPAQUE_SUB":"subject_example"}
SHADOW_ENVIRONMENT_ID=production
SHADOW_OIDC_AUTHORIZATION_URL=https://id.example.com/authorize
SHADOW_OIDC_TOKEN_URL=https://id.example.com/oauth/token
SHADOW_OIDC_CLIENT_ID=REPLACE_CLIENT_ID
SHADOW_OIDC_REDIRECT_URI=https://life.example.com/auth/callback
SHADOW_WEB_ORIGIN=https://life.example.com
SHADOW_SESSION_SECRET=REPLACE_WITH_RANDOM_32_BYTE_VALUE
# Optional NDJSON Agent Runtime gateway. Token stays in local runtime configuration.
SHADOW_RUNTIME_URL=https://example.com/shadow-runtime
SHADOW_RUNTIME_TOKEN=REPLACE_RUNTIME_TOKEN
# Authenticated native project directory. Prefer an actual local operations/runtime file in deployments.
# SHADOW_PROJECT_LINKS_FILE=/absolute/path/to/operations/runtime/project-links.json
SHADOW_PROJECT_LINKS={"schema_version":1,"catalog_revision":"example-1","items":[{"id":"shadow-foliant","title":"股票研究","subtitle":"Shadow Foliant · 独立应用","icon":"chart-line","state":"configured","target":{"kind":"app_link","url":"https://foliant.example.com","package_name":"com.shadow.foliant","web_fallback_url":"https://foliant.example.com"},"auth_hint":"project_managed","order":10}]}
# Development only: SHADOW_ACCESS_TOKEN=dev:subject_example
