import { createInterface } from "node:readline";
import { capabilityRegistry } from "@shadow/contracts";
import { z } from "zod";

const api=(process.env.SHADOW_API_URL??"http://127.0.0.1:8787").replace(/\/$/u,"");
const token=process.env.SHADOW_ACCESS_TOKEN;
const reply=(id:unknown,result?:unknown,error?:unknown)=>process.stdout.write(`${JSON.stringify({jsonrpc:"2.0",id,...(error?{error}:{result})})}\n`);
async function visibleTools():Promise<unknown[]>{if(!token)throw new Error("SHADOW_ACCESS_TOKEN is required");const response=await fetch(`${api}/api/capabilities`,{headers:{authorization:`Bearer ${token}`}});if(!response.ok)throw new Error(`Capability discovery failed with HTTP ${response.status}`);const body=await response.json() as {capabilities:{name:string;description:string}[]};return Promise.all(body.capabilities.map(async item=>{const detail=await fetch(`${api}/api/capabilities/${encodeURIComponent(item.name)}`,{headers:{authorization:`Bearer ${token}`}});if(!detail.ok)throw new Error(`Capability schema failed with HTTP ${detail.status}`);const capability=await detail.json() as {input_schema:unknown};const registered=capabilityRegistry[item.name as keyof typeof capabilityRegistry];return{name:item.name,description:item.description,inputSchema:registered?.idempotency==="required"?{type:"object",additionalProperties:false,required:["command_id","input"],properties:{command_id:{type:"string",description:"Stable idempotency key"},input:capability.input_schema}}:capability.input_schema};}));}
async function call(name:string,args:unknown):Promise<unknown>{if(!token)throw new Error("SHADOW_ACCESS_TOKEN is required");const capability=capabilityRegistry[name as keyof typeof capabilityRegistry];if(!capability)throw new Error("Unknown capability");const headers={authorization:`Bearer ${token}`,"content-type":"application/json"};let response:Response;if(capability.idempotency==="required"){const parsed=z.object({command_id:z.string(),input:z.unknown()}).strict().parse(args);response=await fetch(`${api}/api/commands/${encodeURIComponent(name)}`,{method:"POST",headers,body:JSON.stringify({protocol:"shadow.command",capability:name,...parsed})});}else{const input=capability.inputSchema.parse(args);if(name==="life.list_meals")response=await fetch(`${api}/api/meals?limit=${encodeURIComponent(String((input as {limit:number}).limit))}`,{headers});else if(name==="life.food_catalog"){const value=input as {query?:string;limit:number},search=new URLSearchParams({limit:String(value.limit)});if(value.query)search.set("q",value.query);response=await fetch(`${api}/api/life/foods?${search}`,{headers});}else if(name==="money.summarize")response=await fetch(`${api}/api/money/summary`,{headers});else if(name==="money.records"||name==="health.records"||name==="travel.records"||name==="library.records"){const domain=name.split(".")[0];const value=input as {query?:string;limit:number;cursor?:string};const search=new URLSearchParams({limit:String(value.limit)});if(value.query)search.set("q",value.query);if(value.cursor)search.set("cursor",value.cursor);response=await fetch(`${api}/api/${domain}?${search}`,{headers});}else if(name==="health.trend"){const value=input as {metric_key:string;from?:string;to?:string;limit:number};const search=new URLSearchParams({metric_key:value.metric_key,limit:String(value.limit)});if(value.from)search.set("from",value.from);if(value.to)search.set("to",value.to);response=await fetch(`${api}/api/health/trend?${search}`,{headers});}else if(name==="health.sources")response=await fetch(`${api}/api/health/sources`,{headers});else if(name==="life.get_record"){const value=input as {id:string;sections?:string[]},search=new URLSearchParams();if(value.sections?.length)search.set("sections",value.sections.join(","));response=await fetch(`${api}/api/life/records/${encodeURIComponent(value.id)}${search.size?`?${search}`:""}`,{headers});}else if(name==="money.planning")response=await fetch(`${api}/api/money/planning?period=${encodeURIComponent((input as {period:string}).period)}`,{headers});else if(name==="health.daily")response=await fetch(`${api}/api/health/daily/${encodeURIComponent((input as {date:string}).date)}`,{headers});else if(name==="travel.get_trip")response=await fetch(`${api}/api/travel/trips/${encodeURIComponent((input as {id:string}).id)}`,{headers});else if(name==="library.get_item")response=await fetch(`${api}/api/library/items/${encodeURIComponent((input as {id:string}).id)}`,{headers});else if(name==="operations.get")response=await fetch(`${api}/api/operations/${encodeURIComponent((input as {execution_id:string}).execution_id)}`,{headers});else throw new Error("Query capability is not wired");}const body=await response.json();if(!response.ok)throw new Error(JSON.stringify(body));return body;}
const lines=createInterface({input:process.stdin,crlfDelay:Infinity});
for await(const line of lines){if(!line.trim())continue;let message:Record<string,unknown>;try{message=JSON.parse(line) as Record<string,unknown>;}catch{continue;}try{if(message.method==="initialize")reply(message.id,{protocolVersion:"2025-06-18",capabilities:{tools:{listChanged:false}},serverInfo:{name:"shadow-life",version:"0.1.0"}});else if(message.method==="notifications/initialized")continue;else if(message.method==="tools/list")reply(message.id,{tools:await visibleTools()});else if(message.method==="tools/call"){const params=message.params as {name:string;arguments:unknown};const result=await call(params.name,params.arguments);reply(message.id,{content:[{type:"text",text:JSON.stringify(result)}],structuredContent:result});}else reply(message.id,undefined,{code:-32601,message:"Method not found"});}catch(error){reply(message.id,undefined,{code:-32000,message:error instanceof Error?error.message:"MCP call failed"});}}
